# PDF RAG 시스템 실행 가이드

## 📋 실행 전 확인사항

1. ✅ Ollama가 실행 중인지 확인
   ```bash
   ollama list
   ```

2. ✅ PDF 파일이 `pdfs/` 폴더에 있는지 확인

## 🚀 실행 방법

### 1단계: PDF 파일 처리 (이미 완료됨)
```bash
python main.py --process-dir pdfs
```

### 2단계: 질의응답 시작

#### 방법 A: 대화형 모드 (가장 간단)
```bash
python main.py --query
```

실행 예시:
```
질문: AI의 주요 기술은 무엇인가요?
[INFO] 답변 생성 중...
[ANSWER] 답변:
AI의 주요 기술로는 머신러닝, 딥러닝, 자연어 처리 등이 있습니다...
```

종료: `quit` 또는 `exit` 입력

#### 방법 B: Gradio 웹 UI
```bash
python gradio_app.py
```
→ 브라우저에서 `http://localhost:7860` 접속

#### 방법 C: FastAPI 서버
```bash
python app.py
```
→ 브라우저에서 `http://localhost:8000/docs` 접속

## 💡 사용 팁

1. **질문 예시:**
   - "AI 에이전트의 주요 특징은?"
   - "산업 전망 보고서의 핵심 내용은?"
   - "KPMG 보고서에서 언급한 AI 기술은?"

2. **시스템 정보 확인:**
   ```bash
   python main.py --info
   ```

3. **새로운 PDF 추가:**
   - PDF를 `pdfs/` 폴더에 넣고
   - `python main.py --process-dir pdfs` 다시 실행

## ⚠️ 문제 해결

- **Ollama 연결 오류**: Ollama가 실행 중인지 확인
- **모델 오류**: `ollama pull llama3` 실행
- **인코딩 오류**: Windows 콘솔에서 한글이 깨지면 UTF-8 설정 확인

